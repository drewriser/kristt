# ⚠️ 关键信息缺失说明

✅ **提交接口 (POST):** 文档已获取。
✅ **状态查询接口 (GET):** 文档已获取 (`GET /v1/tasks/{id}`)。
❌ **历史列表接口 (GET List):** 仍然缺失。

为了彻底修复“同步历史”功能，我需要知道如何获取任务列表。

### 缺失的部分：
请提供 **获取任务列表 (List Tasks/History)** 的接口文档或响应示例。

*   **可能的模式：** `GET https://api.apimart.ai/v1/tasks` (最有可能)
*   **或者：** `GET https://api.apimart.ai/v1/history`

请提供该 GET 请求返回的 **JSON 响应结构** (例如：`{ data: [...] }` 还是 `{ list: [...] }`)。

目前程序会尝试猜测此路径，但如果有确切文档，同步将更加稳定。